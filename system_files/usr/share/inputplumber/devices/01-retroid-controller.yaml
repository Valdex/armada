# Based on ROCKNIX 20260701 commit 3e4ee5852e6ca5ea73a38369d2639fad2262648b;
# Armada narrows the profile to the exact RP Mini V2 DT model.
# projects/ROCKNIX/devices/SM8250/filesystem/usr/share/inputplumber/devices/01-retroid-controller.yaml
# yaml-language-server: $schema=https://raw.githubusercontent.com/ShadowBlip/InputPlumber/main/rootfs/usr/share/inputplumber/schema/composite_device_v1.json
# Schema version number
version: 1

# The type of configuration schema
kind: CompositeDevice

# Name of the composite device mapping
name: Retroid Layout

# Only use this profile if *any* of the given matches matches. If this list is
# empty, then the source devices will *always* be checked.
# /sys/class/dmi/id/product_name
matches:
  - udev:
      sys_path: /sys/firmware/devicetree/base
      attributes:
        - name: model
          value: Retroid Pocket Mini V2

# Only allow a single source device per composite device of this type.
single_source: false

# One or more source devices to combine into a single virtual device. The events
# from these devices will be watched and translated according to the key map.
source_devices:
  - group: gamepad
    evdev:
      phys_path: retroid-pocket-gamepad/input0
      handler: event*
    capability_map_id: retroid_mcu
  # SSC gyro bridge (rpmini-sensors.service feeds this IIO device; the name is
  # chosen so InputPlumber's iio_imu driver binds via its bmi* glob).
  - group: imu
    iio:
      name: bmi323-imu

# Optional configuration for the composite device
options:
  # If true, InputPlumber will automatically try to manage the input device. If
  # this is false, InputPlumber will not try to manage the device unless an
  # external service enables management of the device. Defaults to 'false'
  auto_manage: true

# The target input device(s) to emulate by default
target_devices:
  - deck-uhid
  - keyboard
